# 🏗 Архитектура системы дизайн-токенов

## Принцип работы

### Уровень 1: Глобальные токены (Global Color Tokens)

**Источник истины** - базовые цвета для всех брендов

```css
:root {
  /* Светлая тема */
  --states-bg-primary-standard: #f5f5f6;
  --states-text-color-standard: #272727;
  --states-border-secondary-standard: #bcbcbd;
}

:root[data-theme="dark"] {
  /* Темная тема */
  --states-bg-primary-standard: #214bac;
  --states-text-color-standard: #cfcfcf;
  --states-border-secondary-standard: #505254;
}
```

### Уровень 2: Токены брендов (Brand Tokens)

**Ссылаются на глобальные** - семантические имена для компонентов

```css
:root {
  /* ARES kudo использует глобальные токены */
  --kudo-button-bg-standard: var(--states-bg-secondary-standard);
  --kudo-button-text-standard: var(--states-text-color-standard);
  --kudo-button-border-standard: var(--states-border-secondary-standard);
}
```

### Уровень 3: Компоненты

**Используют токены брендов**

```css
.kudo-button {
  background-color: var(--kudo-button-bg-standard);
  color: var(--kudo-button-text-standard);
  border: 1px solid var(--kudo-button-border-standard);
}
```

## Преимущества архитектуры

### ✅ Автоматическая темизация

При переключении темы меняются только глобальные токены:

```javascript
document.documentElement.setAttribute("data-theme", "dark");
```

**Результат**: Все компоненты автоматически получают новые цвета через цепочку ссылок:

```
Глобальные токены (меняются)
  ↓
Токены брендов (автоматически обновляются через var())
  ↓
Компоненты (автоматически обновляются)
```

### ✅ Консистентность между брендами

Разные бренды используют одни и те же глобальные токены:

```css
/* ARES kudo */
--kudo-button-bg-standard: var(--states-bg-secondary-standard);

/* ARES Commander */
--commander-button-bg-standard: var(--states-bg-secondary-standard);

/* DraftSight */
--draftsight-button-bg-standard: var(--states-bg-tertiary-standard);
```

### ✅ Легкая кастомизация

Бренд может переопределить конкретный токен:

```css
:root {
  /* По умолчанию использует глобальный */
  --kudo-button-bg-active: var(--states-bg-primary-hover);
}

/* Специфичное переопределение для темной темы */
:root[data-theme="dark"] {
  --kudo-button-bg-active: #custom-color;
}
```

### ✅ Минимум дублирования

Вместо дублирования значений:

```css
/* ❌ Плохо - дублирование */
--kudo-button-text-standard: #272727;
--kudo-ribbon-text-standard: #272727;
--kudo-tooltip-text: #272727;
```

Используем ссылки:

```css
/* ✅ Хорошо - переиспользование */
--kudo-button-text-standard: var(--states-text-color-standard);
--kudo-ribbon-text-standard: var(--states-text-color-standard);
--kudo-tooltip-text: var(--states-text-color-standard);
```

## Схема наследования

```
tokens.json
    │
    ├─→ globalColorTokens (47 токенов)
    │       │
    │       ├─→ states-bg-* (фоны)
    │       ├─→ states-text-* (текст)
    │       ├─→ states-border-* (границы)
    │       └─→ states-icon-* (иконки)
    │
    └─→ aRESkudo (74 токена)
            │
            ├─→ kudo-button-* → var(--states-*)
            ├─→ kudo-ribbon-* → var(--states-*)
            ├─→ kudo-split-arrow-* → var(--states-*)
            └─→ kudo-tooltip-* → var(--states-*)
```

## Пример: Кнопка в разных темах

### Светлая тема

```
Пользователь видит кнопку → #f5f5f6 (светло-серый фон)
    ↑
.kudo-button { background: var(--kudo-button-bg-standard) }
    ↑
--kudo-button-bg-standard: var(--states-bg-secondary-standard)
    ↑
--states-bg-secondary-standard: #f5f5f6
```

### Темная тема

```
Пользователь видит кнопку → #333538 (темно-серый фон)
    ↑
.kudo-button { background: var(--kudo-button-bg-standard) }
    ↑
--kudo-button-bg-standard: var(--states-bg-secondary-standard)
    ↑
:root[data-theme="dark"] { --states-bg-secondary-standard: #333538 }
```

## Добавление нового бренда

### 1. Определите токены в tokens.json

```json
{
  "newBrand": {
    "buttonBackground": {
      "light": "#color",
      "dark": "#color"
    }
  }
}
```

### 2. Создайте CSS переменные

```css
:root {
  /* Используйте глобальные токены */
  --new-brand-button-bg: var(--states-bg-secondary-standard);
  --new-brand-button-text: var(--states-text-color-standard);
}
```

### 3. Создайте компоненты

```css
.new-brand-button {
  background-color: var(--new-brand-button-bg);
  color: var(--new-brand-button-text);
}
```

**Готово!** Новый бренд автоматически поддерживает обе темы.

## Переопределение для специфичных случаев

Если бренду нужен уникальный цвет:

```css
:root {
  /* Обычно используем глобальный */
  --kudo-button-bg-standard: var(--states-bg-secondary-standard);

  /* Но для active состояния - уникальный цвет */
  --kudo-button-bg-active: #d5e8f2; /* специфичный для kudo */
}

:root[data-theme="dark"] {
  /* В темной теме тоже уникальный */
  --kudo-button-bg-active: #a6b7de;
}
```

## Лучшие практики

### ✅ DO (Делайте)

- Используйте `var()` для ссылок на глобальные токены
- Группируйте токены по компонентам
- Документируйте специфичные переопределения
- Тестируйте в обеих темах

### ❌ DON'T (Не делайте)

- Не дублируйте значения цветов
- Не используйте хардкод цветов в компонентах
- Не создавайте токены без привязки к глобальным
- Не забывайте про темную тему

## Отладка

### Проверить цепочку наследования

```javascript
// В консоли браузера
const styles = getComputedStyle(document.documentElement);
console.log(
  "Global:",
  styles.getPropertyValue("--states-bg-secondary-standard")
);
console.log("kudo:", styles.getPropertyValue("--kudo-button-bg-standard"));
```

### Визуализировать токены

Используйте демо-страницу: http://localhost:5173

## Производительность

CSS переменные с `var()` не влияют на производительность:

- ✅ Вычисляются один раз при рендере
- ✅ Браузер кеширует значения
- ✅ Изменение темы = изменение одного атрибута
- ✅ Каскадное обновление происходит мгновенно

## Итог

Эта архитектура обеспечивает:

1. **Единый источник истины** - globalColorTokens
2. **Автоматическую темизацию** - через var() ссылки
3. **Масштабируемость** - легко добавить новый бренд
4. **Консистентность** - все бренды используют одну базу
5. **Гибкость** - возможность переопределения при необходимости
