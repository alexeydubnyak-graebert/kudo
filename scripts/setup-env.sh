#!/bin/bash

# Скрипт автоматической настройки .env файла
# Автоматически определяет домашнюю директорию пользователя

echo "🔧 Настройка окружения ARES kudo..."

# Проверяем, существует ли .env файл
if [ -f .env ]; then
    echo "⚠️  Файл .env уже существует"
    read -p "Перезаписать? (y/n) " -n 1 -r
    echo
    if [[ ! $REPLY =~ ^[Yy]$ ]]; then
        echo "❌ Отменено"
        exit 1
    fi
fi

# Создаем .env файл с автоматическим определением HOME
cat > .env << EOF
# Локальная конфигурация путей
# Автоматически сгенерировано скриптом setup-env.sh

# Путь к репозиторию с иконками (использует домашнюю директорию)
VITE_ICONS_PATH=~/Documents/GitHub/application-icons
EOF

echo "✅ Файл .env создан успешно!"
echo "📁 Путь к иконкам: ~/Documents/GitHub/application-icons"
echo ""
echo "Если репозиторий application-icons находится в другом месте,"
echo "отредактируйте файл .env вручную"
