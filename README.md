# ARES kudo

Мультибрендовая дизайн-система и интерфейс для ARES kudo, созданный с использованием React и Vite.

## 🚀 Быстрый старт

### Первый запуск на новом компьютере

```bash
# Установка зависимостей
npm install

# Настройка окружения (создание .env)
./scripts/setup-env.sh

# Обновление путей к иконкам и запуск
npm run setup
```

### Обычный запуск

```bash
npm run dev
```

Приложение будет доступно по адресу: http://localhost:5173

### Обновление путей к иконкам

Если вы открыли проект на другом компьютере и пути к иконкам не работают:

```bash
npm run update-paths
```

Подробнее: [ICON_PATHS.md](./ICON_PATHS.md)

### Сборка для продакшена

```bash
npm run build
```

### Предварительный просмотр сборки

```bash
npm run preview
```

## 🎨 Дизайн-система

### Генерация токенов

```bash
# Сгенерировать CSS переменные из tokens.json
npm run generate:tokens

# Автоматическая регенерация при изменении tokens.json
npm run tokens:watch
```

### Документация

- **[ARCHITECTURE.md](./ARCHITECTURE.md)** - Архитектура системы токенов
- **[DESIGN_SYSTEM.md](./DESIGN_SYSTEM.md)** - Полная документация дизайн-системы
- **[TOKENS_README.md](./TOKENS_README.md)** - Руководство по работе с токенами
- **[src/components/README.md](./src/components/README.md)** - Документация компонентов
- **[SUMMARY.md](./SUMMARY.md)** - Резюме проделанной работы

## 📁 Структура проекта

```
ares-kudo/
├── src/
│   ├── components/        # React компоненты
│   ├── styles/
│   │   ├── tokens.css     # CSS переменные из tokens.json
│   │   └── base.css       # Базовые стили
│   ├── utils/
│   │   └── theme.js       # Утилиты для работы с темами
│   ├── App.jsx            # Главный компонент
│   ├── App.css            # Стили приложения
│   ├── main.jsx           # Точка входа
│   └── index.css          # Импорт стилей
├── scripts/
│   └── generate-tokens.js # Генератор CSS из tokens.json
├── tokens.json            # Дизайн-токены (исходник)
├── tokens-formatted.json  # Отформатированная версия
└── public/                # Статические файлы
```

## 🎯 Особенности

### Мультибрендовая система с переиспользованием

- ✅ **Трехуровневая архитектура**: Глобальные токены → Токены брендов → Компоненты
- ✅ **Переиспользование через var()**: Токены брендов ссылаются на глобальные
- ✅ **Автоматическая темизация**: Изменение глобальных токенов → каскадное обновление
- ✅ **47 глобальных токенов** для всех брендов
- ✅ **74 токена ARES kudo** (ссылаются на глобальные)
- ✅ Поддержка ARES Commander и DraftSight
- ✅ Легкое добавление новых брендов

### Темизация

- ✅ Светлая и темная тема
- ✅ Автоматическое определение системной темы
- ✅ Сохранение выбора пользователя
- ✅ Плавные переходы между темами

### Состояния компонентов

Все интерактивные элементы поддерживают 5 состояний:

- **Standard** - обычное состояние
- **Hover** - при наведении курсора
- **Pressed** - при нажатии
- **Active** - активное/выбранное состояние
- **Disabled** - неактивное состояние

## 🛠 Технологии

- **React 18** - UI библиотека
- **Vite** - Сборщик и dev-сервер
- **CSS Variables** - Дизайн-токены
- **ESLint** - Линтер кода

## 📚 Использование токенов

### В CSS

```css
.my-button {
  background-color: var(--kudo-button-bg-standard);
  color: var(--kudo-button-text-standard);
  border: 1px solid var(--kudo-button-border-standard);
}

.my-button:hover {
  background-color: var(--kudo-button-bg-hover);
}
```

### В React

```jsx
import { useTheme } from "./utils/theme";

function MyComponent() {
  const { theme, toggleTheme } = useTheme();

  return <button onClick={toggleTheme}>Текущая тема: {theme}</button>;
}
```

## 🎨 Переключение темы

### JavaScript

```javascript
// Установить темную тему
document.documentElement.setAttribute("data-theme", "dark");

// Установить светлую тему
document.documentElement.setAttribute("data-theme", "light");

// Или использовать утилиты
import { setTheme, toggleTheme } from "./utils/theme";

setTheme("dark");
toggleTheme();
```

## 🔧 Разработка

### Добавление новых токенов

1. Обновите `tokens.json`
2. Запустите `npm run generate:tokens`
3. Используйте новые CSS переменные

### Создание компонентов

1. Создайте папку в `src/components/`
2. Используйте токены из `tokens.css`
3. Следуйте соглашениям из `src/components/README.md`

## 📊 Статистика токенов

- **Global Color Tokens**: 47 токенов
- **ARES kudo**: 74 токена
- **ARES Commander**: 67 токенов
- **DraftSight**: 106 токенов
- **Global Palette**: 482 цвета

## 🤝 Вклад

При добавлении новых токенов или компонентов:

1. Следуйте существующим соглашениям об именовании
2. Обновляйте документацию
3. Тестируйте в обеих темах (светлой и темной)
4. Проверяйте все 5 состояний компонентов

## 📝 Лицензия

Proprietary - ARES kudo
