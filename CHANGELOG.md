# Changelog

## [Улучшение архитектуры] - 2025-10-11

### 🎯 Переработана архитектура системы токенов

#### Изменения

Переход от дублирования значений к переиспользованию через `var()` ссылки.

#### Было (❌ Дублирование)

```css
:root {
  --kudo-button-bg-standard: #f1f1f1;
  --kudo-button-text-standard: #3d3d3d;
}

:root[data-theme="dark"] {
  --kudo-button-bg-standard: #f5f5f6;
  --kudo-button-text-standard: #272727;
}
```

**Проблемы:**

- Дублирование значений цветов
- Сложно поддерживать консистентность
- Нужно обновлять каждый токен при изменении темы
- Нет связи между глобальными и специфичными токенами

#### Стало (✅ Переиспользование)

```css
/* Глобальные токены */
:root {
  --states-bg-secondary-standard: #f5f5f6;
  --states-text-color-standard: #272727;
}

:root[data-theme="dark"] {
  --states-bg-secondary-standard: #333538;
  --states-text-color-standard: #cfcfcf;
}

/* Токены брендов ссылаются на глобальные */
:root {
  --kudo-button-bg-standard: var(--states-bg-secondary-standard);
  --kudo-button-text-standard: var(--states-text-color-standard);
}

/* Темная тема работает автоматически через var() */
```

**Преимущества:**

- ✅ Нет дублирования - единый источник истины
- ✅ Автоматическая темизация через цепочку ссылок
- ✅ Легко поддерживать консистентность между брендами
- ✅ Изменение глобального токена → каскадное обновление всех компонентов

### 📊 Трехуровневая архитектура

```
Уровень 1: Глобальные токены (--states-*)
    ↓ var()
Уровень 2: Токены брендов (--kudo-*, --commander-*)
    ↓ var()
Уровень 3: Компоненты (.button, .ribbon, etc.)
```

### 🔄 Как работает переключение темы

**Старая архитектура:**

```
Переключение темы → Обновить ВСЕ токены вручную
```

**Новая архитектура:**

```
Переключение темы → Глобальные токены меняются
                  → Токены брендов автоматически обновляются (var())
                  → Компоненты автоматически перекрашиваются
```

### 📝 Обновленные файлы

#### Изменено

- `src/styles/tokens.css` - переработаны все токены kudo для использования var()
- `README.md` - обновлена документация с новой архитектурой
- `SUMMARY.md` - добавлено описание трехуровневой системы

#### Добавлено

- `ARCHITECTURE.md` - подробная документация архитектуры
- `src/components/TokenInspector.jsx` - визуальная демонстрация цепочки наследования
- `src/components/TokenInspector.css` - стили для инспектора
- `CHANGELOG.md` - этот файл

### 🎨 Примеры использования

#### Создание компонента

```css
.my-component {
  /* Используйте токены бренда */
  background-color: var(--kudo-button-bg-standard);
  color: var(--kudo-button-text-standard);
  border: 1px solid var(--kudo-button-border-standard);
}

.my-component:hover {
  background-color: var(--kudo-button-bg-hover);
}
```

#### Добавление нового бренда

```css
:root {
  /* Новый бренд ссылается на глобальные токены */
  --new-brand-button-bg: var(--states-bg-secondary-standard);
  --new-brand-button-text: var(--states-text-color-standard);
}

/* Темная тема работает автоматически! */
```

### 🚀 Производительность

- ✅ CSS переменные вычисляются один раз
- ✅ Браузер кеширует значения
- ✅ Изменение темы = изменение одного атрибута
- ✅ Каскадное обновление происходит мгновенно

### 📚 Документация

Для подробной информации см.:

- [ARCHITECTURE.md](./ARCHITECTURE.md) - архитектура системы
- [TOKENS_README.md](./TOKENS_README.md) - руководство по токенам
- [DESIGN_SYSTEM.md](./DESIGN_SYSTEM.md) - дизайн-система

---

## [Начальная версия] - 2025-10-10

### Создано

- Базовая структура проекта на React + Vite
- Система дизайн-токенов из tokens.json
- Поддержка светлой и темной темы
- Демонстрационная страница
- Генератор CSS из JSON
- Полная документация
