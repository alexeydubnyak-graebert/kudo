# RibbonItem Component

Компонент элемента ленты для интерфейса ARES kudo, созданный на основе дизайна из Figma.

## 🎯 Особенности

- ✅ **5 состояний**: standard, hover, pressed, active, disabled
- ✅ **Полное соответствие Figma**: все размеры и цвета из дизайна
- ✅ **Модульные токены**: использует трехуровневую систему токенов
- ✅ **Темная тема**: автоматическая поддержка через токены
- ✅ **Доступность**: ARIA атрибуты, keyboard navigation
- ✅ **TypeScript ready**: PropTypes для валидации

## 📦 Использование

### Базовый пример

```jsx
import RibbonItem from "./components/RibbonItem";
import CurrentIcon from "./components/icons/CurrentIcon";

function MyComponent() {
  return (
    <RibbonItem
      icon={<CurrentIcon />}
      label="Текущий"
      onClick={() => console.log("clicked")}
    />
  );
}
```

### Активное состояние

```jsx
<RibbonItem icon={<CurrentIcon />} label="Активный" active={true} />
```

### Отключенное состояние

```jsx
<RibbonItem icon={<CurrentIcon />} label="Отключен" disabled={true} />
```

### Группа элементов

```jsx
function RibbonGroup() {
  const [active, setActive] = useState("item1");

  return (
    <div style={{ display: "flex", gap: "4px" }}>
      <RibbonItem
        icon={<Icon1 />}
        label="Элемент 1"
        active={active === "item1"}
        onClick={() => setActive("item1")}
      />
      <RibbonItem
        icon={<Icon2 />}
        label="Элемент 2"
        active={active === "item2"}
        onClick={() => setActive("item2")}
      />
    </div>
  );
}
```

## 🎨 Props

| Prop        | Type     | Default | Описание                          |
| ----------- | -------- | ------- | --------------------------------- |
| `icon`      | `node`   | -       | Иконка компонента (React элемент) |
| `label`     | `string` | -       | Текст метки                       |
| `active`    | `bool`   | `false` | Активное состояние                |
| `disabled`  | `bool`   | `false` | Отключенное состояние             |
| `onClick`   | `func`   | -       | Обработчик клика                  |
| `className` | `string` | `''`    | Дополнительные CSS классы         |

## 🎨 Используемые токены

### Цвета

```css
/* Backgrounds */
--kudo-ribbon-item-bg-standard: #f5f5f6
--kudo-ribbon-item-bg-hover: #e2e2e2
--kudo-ribbon-item-bg-pressed: #cccccc
--kudo-ribbon-item-bg-active: #d5ddef
--kudo-ribbon-item-bg-disabled: #f5f5f6

/* Text */
--kudo-ribbon-item-text-standard: #272727
--kudo-ribbon-item-text-active: #050505
--kudo-ribbon-item-text-disabled: #898b8c

/* Icons */
--kudo-ribbon-item-icon-standard: #272727
--kudo-ribbon-item-icon-active: #050505
--kudo-ribbon-item-icon-disabled: #898b8c
```

### Размеры

```css
--kudo-item-padding: 6px
--kudo-item-margin: 6px
--kudo-item-border-radius: 4px
--size-6: 6px (gap между иконкой и текстом)
```

### Типографика

```css
--font-family-kudo: 'Roboto', ...
--font-size-lg: 14px
--font-weight-md: 400
--line-height-md: 16px
```

## 🔄 Цепочка переиспользования токенов

```
--kudo-ribbon-item-bg-standard
  ↓ ссылается на
--states-bg-secondary-standard
  ↓ ссылается на
--palette-jango-tint-28
  ↓ равен
#f5f5f6
```

В темной теме автоматически меняется на `#333538` благодаря переопределению глобальных токенов.

## ♿ Доступность

- **ARIA атрибуты**: `aria-pressed`, `aria-disabled`
- **Keyboard navigation**: Enter и Space для активации
- **Focus visible**: видимый outline при фокусе с клавиатуры
- **Semantic HTML**: использует `<button>` элемент

## 🎭 Состояния

### Standard

Обычное состояние по умолчанию

### Hover

Появляется при наведении курсора (`:hover`)

### Pressed

Появляется при нажатии (`:active`)

### Active

Выбранный/активный элемент (prop `active={true}`)

### Disabled

Отключенное состояние (prop `disabled={true}`)

## 📱 Адаптивность

Компонент адаптируется к размеру контента:

- Иконка: фиксированный размер 16×16px
- Текст: автоматическая ширина
- Общая высота: 28px (из Figma)

## 🌙 Темная тема

Все цвета автоматически меняются при переключении темы:

```javascript
document.documentElement.setAttribute("data-theme", "dark");
```

Не требуется дополнительных стилей - всё работает через систему токенов!

## 📝 Примечания

- Компонент создан на основе дизайна из Figma (node-id: 5099:5322-5334)
- Все значения полностью соответствуют дизайну
- Использует модульную систему токенов с трехуровневой иерархией
- Поддерживает все интерактивные состояния из дизайна

## 🚀 Демо

Запустите приложение и перейдите на главную страницу, чтобы увидеть все варианты компонента в действии.

```bash
npm run dev
```

Откройте http://localhost:5173
