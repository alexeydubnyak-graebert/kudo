import React from 'react';
import './Viewport.css';

const Viewport = () => {
    return (
        <div className="viewport">
            {/* Черный экран для viewport */}
        </div>
    );
};

export default Viewport;
