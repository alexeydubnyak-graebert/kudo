# 🎨 Модульная архитектура токенов

## 📁 Структура файлов

```
src/styles/tokens/
├── global-palette.css      # Уровень 1: Базовые палитры цветов
├── global-tokens.css       # Уровень 2: Семантические токены (используют палитры)
├── dimensions.css          # Уровень 2: Размеры
├── typography.css          # Уровень 2: Типографика
├── ares-kudo.css          # Уровень 3: Токены бренда kudo (используют глобальные)
└── ares-commander.css     # Уровень 3: Токены бренда Commander (используют глобальные)
```

## 🎯 Иерархия переиспользования

```
┌─────────────────────────────────────────────────────────────┐
│ Уровень 1: ПАЛИТРЫ (global-palette.css)                    │
│ Чистые цвета без семантики                                  │
│ --palette-vader-tint-1: #141518                            │
│ --palette-obi-tint-24: #d5ddef                             │
│ --palette-jango-tint-28: #f5f5f6                           │
└─────────────────────────────────────────────────────────────┘
                            ↓ используют
┌─────────────────────────────────────────────────────────────┐
│ Уровень 2: ГЛОБАЛЬНЫЕ ТОКЕНЫ (global-tokens.css)           │
│ Семантические токены с переиспользованием палитр            │
│ --states-bg-primary-standard: var(--palette-jango-tint-28) │
│ --states-text-color-standard: var(--palette-rey-shadow-24) │
│ --states-border-secondary-standard: var(--palette-jango-tint-20) │
└─────────────────────────────────────────────────────────────┘
                            ↓ используют
┌─────────────────────────────────────────────────────────────┐
│ Уровень 3: ТОКЕНЫ БРЕНДОВ (ares-kudo.css)                  │
│ Специфичные для бренда, используют глобальные токены        │
│ --kudo-ribbon-item-bg-standard: var(--states-bg-secondary-standard) │
│ --kudo-ribbon-item-text-standard: var(--states-text-color-standard) │
│ --kudo-item-margin: var(--size-6)                          │
└─────────────────────────────────────────────────────────────┘
```

## 📊 Детальное описание файлов

### 1. `global-palette.css` - Базовые палитры

**Назначение**: Самый низкий уровень - чистые цвета без семантики

**Палитры**:

- **VADER** - Темные оттенки (от #141518 до #ffffff)
- **OBI** - Синие оттенки (от #214bac до #ffffff)
- **JANGO** - Серые оттенки (от #333538 до #ffffff)
- **SNOKE** - Очень темные (#1e2023)
- **CLONE** - Нейтральные серые (#646464 - #ffffff)
- **REY** - Светлые серые (#cfcfcf - #ffffff)
- **RED** - Красные (#ed0000 - #ffffff)

**Примеры**:

```css
--palette-vader-tint-1: #141518;
--palette-obi-tint-24: #d5ddef;
--palette-jango-tint-28: #f5f5f6;
--palette-white: #ffffff;
--palette-black: #010101;
```

**Использование**: Напрямую НЕ используются в компонентах, только через глобальные токены.

---

### 2. `global-tokens.css` - Семантические токены

**Назначение**: Семантические токены для состояний, используют палитры

**Категории**:

- `--states-bg-*` - Фоны для разных состояний
- `--states-text-color-*` - Цвета текста
- `--states-icon-color-*` - Цвета иконок
- `--states-border-*` - Цвета границ
- `--background-*` - Специальные фоны
- `--divider-*` - Разделители

**Примеры переиспользования**:

```css
/* Светлая тема */
--states-bg-primary-standard: var(--palette-jango-tint-28); /* #f5f5f6 */
--states-bg-primary-hover: var(--palette-obi-tint-24); /* #d5ddef */
--states-text-color-standard: var(--palette-rey-shadow-24); /* #272727 */

/* Темная тема */
--states-bg-primary-standard: var(--palette-obi-tint-1); /* #214bac */
--states-text-color-standard: var(--palette-rey-tint-1); /* #cfcfcf */
```

**Состояния**:

- `standard` - Обычное состояние
- `hover` - При наведении
- `pressed` - При нажатии
- `active` - Активное состояние
- `disabled` - Отключенное состояние

---

### 3. `dimensions.css` - Размеры

**Назначение**: Базовая система размеров

```css
--size-2: 2px;
--size-4: 4px;
--size-6: 6px;
--size-8: 8px;
--size-10: 10px;
--size-12: 12px;
--size-14: 14px;
/* ... до --size-30 */
```

---

### 4. `typography.css` - Типографика

**Назначение**: Шрифты, размеры, веса

```css
--font-family-kudo: "Roboto", ...;
--font-family-commander: "Segoe UI", ...;

--font-size-sm: 10px;
--font-size-md: 12px;
--font-size-lg: 14px;
--font-size-xl: 16px;

--font-weight-sm: 300;
--font-weight-md: 400;
--font-weight-lg: 900;

--line-height-sm: 12px;
--line-height-md: 16px;
--line-height-lg: 20px;
```

---

### 5. `ares-kudo.css` - Токены бренда kudo

**Назначение**: Специфичные для kudo, переиспользуют глобальные токены

**Примеры переиспользования**:

```css
/* Цвета - используют глобальные токены */
--kudo-ribbon-item-bg-standard: var(--states-bg-secondary-standard);
--kudo-ribbon-item-bg-hover: var(--states-bg-secondary-hover);
--kudo-ribbon-item-text-standard: var(--states-text-color-standard);
--kudo-ribbon-item-icon-standard: var(--states-icon-color-standard);

/* Размеры - используют глобальные размеры */
--kudo-item-margin: var(--size-6);
--kudo-item-padding: var(--size-6);
--kudo-item-border-radius: var(--size-4);

/* Специфичные переопределения в темной теме */
:root[data-theme="dark"] {
  --kudo-dropdown-menu-border: var(--palette-black);
  --kudo-split-arrow-icon-standard: var(--palette-white);
}
```

**Компоненты**:

- Slider
- Tooltip
- Ribbon & Ribbon Item
- Dropdown Menu
- Input
- Split Arrow
- Tab Ribbon

---

### 6. `ares-commander.css` - Токены бренда Commander

**Назначение**: Специфичные для Commander, переиспользуют глобальные токены

Аналогично kudo, но с другими размерами и некоторыми отличиями в поведении.

---

## 🎯 Преимущества модульной архитектуры

### 1. **Четкая иерархия**

```
Палитры → Глобальные токены → Токены брендов → Компоненты
```

### 2. **Переиспользование**

- Изменение в палитре автоматически распространяется на все уровни
- Глобальные токены используются несколькими брендами
- Нет дублирования значений

### 3. **Масштабируемость**

- Легко добавить новый бренд (создать новый файл уровня 3)
- Легко добавить новую палитру
- Легко изменить глобальное поведение

### 4. **Поддерживаемость**

- Каждый файл отвечает за свою область
- Легко найти и изменить нужный токен
- Понятная структура для новых разработчиков

### 5. **Темизация**

- Темная тема переопределяет только необходимые токены
- Автоматическое каскадирование изменений

---

## 💡 Примеры использования

### Создание компонента Ribbon Item

```css
.ribbon-item {
  /* Используем токены kudo уровня 3 */
  margin: var(--kudo-item-margin);
  padding: var(--kudo-item-padding);
  border-radius: var(--kudo-item-border-radius);

  /* Цвета автоматически меняются при смене темы */
  background: var(--kudo-ribbon-item-bg-standard);
  color: var(--kudo-ribbon-item-text-standard);
  border: 1px solid var(--kudo-ribbon-item-border-standard);
}

.ribbon-item:hover {
  background: var(--kudo-ribbon-item-bg-hover);
  border-color: var(--kudo-ribbon-item-border-hover);
}

.ribbon-item.active {
  background: var(--kudo-ribbon-item-bg-active);
  color: var(--kudo-ribbon-item-text-active);
}
```

### Цепочка переиспользования

Когда вы используете `var(--kudo-ribbon-item-bg-standard)`:

```
--kudo-ribbon-item-bg-standard
  ↓ ссылается на
--states-bg-secondary-standard
  ↓ ссылается на
--palette-jango-tint-28
  ↓ равен
#f5f5f6
```

В темной теме:

```
--kudo-ribbon-item-bg-standard
  ↓ ссылается на
--states-bg-secondary-standard
  ↓ ссылается на (в темной теме)
--palette-jango-tint-1
  ↓ равен
#333538
```

---

## 🔄 Переключение темы

```javascript
// Переключить на темную тему
document.documentElement.setAttribute("data-theme", "dark");

// Вернуть светлую тему
document.documentElement.removeAttribute("data-theme");
```

Все токены автоматически обновятся благодаря каскадному переиспользованию!

---

## 📝 Добавление нового токена

### Уровень 1 (Палитра):

```css
/* global-palette.css */
--palette-new-color: #123456;
```

### Уровень 2 (Глобальный токен):

```css
/* global-tokens.css */
--states-new-property: var(--palette-new-color);
```

### Уровень 3 (Бренд):

```css
/* ares-kudo.css */
--kudo-component-property: var(--states-new-property);
```

---

## 🎉 Итог

Модульная архитектура обеспечивает:

- ✅ Четкое разделение ответственности
- ✅ Максимальное переиспользование
- ✅ Легкую поддержку и масштабирование
- ✅ Автоматическую темизацию
- ✅ Единый источник истины для каждого уровня

**Все готово к использованию!** 🚀
