# Управление путями к иконкам

## Проблема

Если в `ribbon-config.xml` используются абсолютные пути с именем пользователя:

```xml
<ribbonItem icon="/Users/alexey/Documents/GitHub/application-icons/ribbon/home/draw/light/line.svg" />
```

То на другом компьютере с другим пользователем эти пути не будут работать.

## Решение

### Автоматическое обновление путей

При открытии проекта на новом компьютере запустите скрипт обновления путей:

```bash
npm run update-paths
```

Скрипт автоматически:

1. ✅ Найдет все пути с `/Users/старое_имя/`
2. ✅ Заменит на `/Users/новое_имя/`
3. ✅ Создаст резервную копию `ribbon-config.xml.backup`
4. ✅ Покажет количество обновленных путей

### Быстрый старт на новом компьютере

Запустите одну команду, которая обновит пути и запустит dev-сервер:

```bash
npm run setup
```

## Примеры использования

### Пример 1: Первый запуск на новом компьютере

```bash
# Клонируем репозиторий
git clone <repo-url>
cd kudo

# Устанавливаем зависимости
npm install

# Обновляем пути и запускаем
npm run setup
```

### Пример 2: Только обновление путей

```bash
npm run update-paths
```

Вывод:

```
🔧 Обновление путей к иконкам...
📁 Текущий пользователь: john
📁 Домашняя директория: /Users/john

📋 Найдено путей для замены: 15
   /Users/alexey/

💾 Создана резервная копия: ribbon-config.xml.backup

✅ Успешно обновлено путей: 15
📝 Все пути теперь используют: /Users/john/

🎉 Готово! Можно запускать проект.
```

## Форматы путей в XML

### ✅ Рекомендуемый: Относительные пути или ~/

```xml
<!-- Только имя файла (рекомендуется) -->
<ribbonItem icon="line.svg" />

<!-- Путь с ~/ (автоматически раскрывается) -->
<ribbonItem icon="~/Documents/GitHub/application-icons/ribbon/home/draw/light/line.svg" />
```

### ⚠️ Абсолютные пути (требуют обновления)

```xml
<!-- Будет обновлено скриптом -->
<ribbonItem icon="/Users/alexey/Documents/GitHub/application-icons/ribbon/home/draw/light/line.svg" />
```

## Резервные копии

Скрипт автоматически создает резервную копию перед изменениями:

- `ribbon-config.xml.backup` - последняя резервная копия

Чтобы откатить изменения:

```bash
cp src/config/ribbon-config.xml.backup src/config/ribbon-config.xml
```

## Troubleshooting

### Скрипт не находит пути для замены

Это нормально, если вы используете относительные пути или `~/`. Скрипт работает только с абсолютными путями `/Users/username/`.

### Пути все еще не работают

1. Проверьте, что репозиторий `application-icons` склонирован
2. Убедитесь, что путь правильный: `~/Documents/GitHub/application-icons`
3. Проверьте структуру папок в репозитории
4. Перезапустите dev-сервер

### Нужно изменить базовый путь

Если репозиторий находится не в `~/Documents/GitHub/application-icons`, отредактируйте `.env`:

```env
VITE_ICONS_PATH=~/путь/к/вашему/application-icons
```

## Лучшие практики

1. **Используйте короткие имена файлов** в XML:

   ```xml
   <ribbonItem icon="line.svg" />
   ```

   Система автоматически найдет иконку в правильной папке.

2. **Не коммитьте абсолютные пути** в Git, если это возможно.

3. **Запускайте `npm run update-paths`** после каждого `git pull` на новом компьютере.

4. **Проверяйте резервные копии** перед коммитом изменений.
